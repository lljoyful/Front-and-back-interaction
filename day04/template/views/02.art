{{if age>18}} 
    年龄大于18 
{{else if age<15}} 
    //只能一个个的传{{name,age}}只输出第一个
    年龄小于15 ，年龄为{{name}}
{{else}}
    年龄不符合要求
{{/if}}

<%if(age>18){%>
    年龄大于18 
<%}else if(age<15){%>
    年龄小于15 ，年龄为{{age}}
<%}else{%>
    年龄不符合要求
<%}%>